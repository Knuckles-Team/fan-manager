[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    concept: bind a test to a CONCEPT id (e.g. @pytest.mark.concept("CONCEPT:FAN-001"))
filterwarnings =
    ignore:.*exclude_args.*
